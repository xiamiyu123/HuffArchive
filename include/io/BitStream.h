#pragma once

#include <string>
#include <vector>

namespace IO {

class BitStream {
public:
    BitStream();
    ~BitStream();

    // TODO: Add bit manipulation methods
    // void writeBit(bool bit);
    // bool readBit();
};

}
