#pragma once

namespace IO {

class DataGenerator {
public:
    DataGenerator();
    ~DataGenerator();

    // TODO: Add random data generation methods
    // static std::string generateRandomText(int length);
};

}
